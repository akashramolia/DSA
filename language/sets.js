/* 

Question : you are given a string, for example "akash", you have to return the number of unique charactors you see
in the string. Here ans is 4, becausethere are 4 unique charactors: a k s h.

Solution: Whenever there is a mention of uniqueness or removal of duplicates,
sets is used. I will put all charactors inside the set and will return the size of the sets.


 */

finduniquecharactors(){
    let str;

    let set = new Set();
    for(let char in str){
        set.push();
    }
    
}